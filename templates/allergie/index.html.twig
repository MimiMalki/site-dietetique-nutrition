{% extends 'base.html.twig' %}

{% block title %}Allergie index{% endblock %}

{% block body %}
    <h1>Allergie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for allergie in allergies %}
            <tr>
                <td>{{ allergie.id }}</td>
                <td>{{ allergie.nom }}</td>
                <td>
                    <a href="{{ path('app_allergie_show', {'id': allergie.id}) }}">show</a>
                    <a href="{{ path('app_allergie_edit', {'id': allergie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_allergie_new') }}">Create new</a>
{% endblock %}
