{% extends 'base.html.twig' %}

{% block title %}Edit Avis{% endblock %}

{% block body %}
<style>
		h1 {
			text-align: center;
			color: #ea5e5e;
			
		}
		a {
			color: #92c83e;
		}
	</style>
    
    <h1 class="text-center">Modifier l'avis</h1>
<div class="flex items-center justify-between mr-12">
    <a class="m-12" href="{{ path('app_avis_index') }}"><i class="fa-solid fa-arrow-left"></i> Retour à la liste des avis</a>
   <div class="mr-12">
        {{ include('avis/_delete_form.html.twig') }}
    </div>
</div>
    {{ include('avis/_form.html.twig', {'button_label': 'Modifier'}) }}

    {# <h1>Edit Avis</h1>

    {{ include('avis/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_avis_index') }}">back to list</a>

    {{ include('avis/_delete_form.html.twig') }} #}
{% endblock %}
